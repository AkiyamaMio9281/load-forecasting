# Core data stack
pandas==2.2.3
numpy==1.26.4
pyarrow==17.0.0
pandera==0.20.4

# Models
statsmodels==0.14.4
# 1.1.6's wheel ships a cmdstan dir without a makefile, which cmdstanpy >=1.2.2
# rejects -- Prophet then silently loses its stan backend. 1.3.0 fixes the packaging.
prophet==1.3.0
lightgbm==4.5.0
scikit-learn==1.5.2

# Calendar / weather
holidays==0.57
requests==2.32.3

# Viz / notebooks
matplotlib==3.9.2
seaborn==0.13.2
jupyter==1.1.1
nbformat==5.10.4

# Serving
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
joblib==1.4.2

# Data source
kaggle==1.6.17

# Dev
pytest==8.3.3
pytest-cov==5.0.0
ruff==0.7.4

# --- Optional: the LSTM comparison only ---------------------------------------
# Not installed by default; the model registry imports torch lazily and every other
# model works without it.
#
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cu128
#
# Pin 2.8.0 deliberately. On Windows the 2.11.0+cu128 wheel fails to load c10.dll
# (WinError 1114) whenever pandas is imported first -- which is always, here.
